{
  "name": "websocket-chat",
  "version": "1.0.0",
  "description": "Websocket chat application with React client and Node.js server",
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../api && npm install",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd api && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\""
  },
  "keywords": ["chat", "websocket", "react", "node"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}